# Requires authentication via radius. Radius config in /etc/raddb/server
auth required /lib/security/pam_radius_auth.so debug

# Accept any user since we're dealing with virtual users there's no need to have a system account (pam_unix.so)
account sufficient pam_permit.so